#pragma once

/**
 * @brief Print a message using dynamic library and return its size
 * 
 * @param msg The message to be printed.
 * @return The message size.
 */
int dynamic_lib_function(char* msg);
